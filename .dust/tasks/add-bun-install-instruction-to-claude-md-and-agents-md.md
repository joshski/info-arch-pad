# Add bun install instruction to CLAUDE.md and AGENTS.md

Add `bun install` as an unconditional step before `bunx dust agent` in both CLAUDE.md and AGENTS.md.

## Goals

(none)

## Blocked By

(none)

## Definition of Done

- [ ] CLAUDE.md includes `bun install` instruction before the `bunx dust agent` line
- [ ] AGENTS.md includes `bun install` instruction before the `bunx dust agent` line
- [ ] `bunx dust check` passes
